IMPORTANT UPDATE — Use TailwindCSS
────────────────────────────────────
Rebuild and style the entire Astro project using **TailwindCSS**.

Requirements:

1. Install & configure Tailwind using the official Astro integration:
   https://docs.astro.build/en/guides/integrations-guide/tailwind/

2. DO NOT manually write large CSS files.
   - Only create a minimal `globals.css` for base resets or custom variables if needed.
   - All layout, spacing, typography, colors, grids, flex, etc. SHOULD use Tailwind utility classes.

3. Define the brand colors in `tailwind.config.js`:

theme: {
extend: {
colors: {
blackIron: “#0A0A0C”,
forgeGray: “#1E1F22”,
dataSilver: “#C3C6CF”,
pureWhite: “#FFFFFF”,
signalCyan: “#00E5FF”,
forgeOrange: “#FF6B35”
}
}
}

4. Use Tailwind classes for:
- responsive layout (`container`, `mx-auto`, `px-4`)
- spacing (`py-12`, `mt-8`)
- typography (`text-4xl`, `font-bold`)
- grid (`grid grid-cols-1 md:grid-cols-3 gap-8`)
- color (`text-signalCyan`, `bg-blackIron`, `border-forgeGray`)
- CTAs (`bg-signalCyan text-blackIron hover:bg-cyan-300`)

5. All components must use Tailwind classes only.  
DO NOT write verbose inline styling or CSS unless absolutely required.

6. In your output, DO NOT generate unnecessary explanations or long descriptions.
Be compact and generate only files + code that is needed. 
Avoid filler text or long placeholder paragraphs.

7. Inside the final code:
- Add comments like:  
  `<!-- TODO: Insert logo here -->`
  where appropriate.

8. Keep total output token size small and optimized.  
Use short dummy text for placeholders.  
I will rewrite content later manually.

────────────────────────────────────
REGENERATE ALL FILES USING TAILWIND.
